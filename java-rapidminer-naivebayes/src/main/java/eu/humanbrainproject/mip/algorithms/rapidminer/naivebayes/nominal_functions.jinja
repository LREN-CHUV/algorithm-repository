"apply": {
    "params": [
        {
            "elem": "string"
        },
        {
            "ds": {"type": "map", "values": "double"}
        }
   ],
   "ret": "double",
   "do": [{"attr": "ds", "path": ["elem"]}]
},

"multiply": {
    "params": [
        {
            "a": "double"
        },
        {
            "b": "double"
        }
    ],
    "ret": "double",
    "do": [{"*": ["a", "b"]}]
},

"toArray": {
   "params": [
      {
         "m": "DependentVariables"
      }
   ],
   "ret": {"type": "array", "items": "string"},
   "do": [
      {"type": {"type": "array", "items": "string"}, "new": [
{% for attr in attributeNames %}
        {"enum.toString": {"attr": "m","path":[{"string":"{{ attr }}"}]}}{{ "," if not loop.last }}
      {% endfor %}
      ]}
   ]
}
