r-box-stats:
  build: Dockerfile
  image: registry.federation.mip.hbp/mip_node/r-box-stats
  pre:
    - echo "Preparing r-box-stats"
  post:
    - echo "Finished r-box-stats"

r-box-stats-test:
  build: Dockerfile.test
  image: registry.federation.mip.hbp/mip_node/r-box-stats-test
  pre:
    - echo "Preparing r-box-stats-test"
  post:
    - echo "Finished r-box-stats-test"
  test:
    - tests/setup.sh
    - tests/test.sh
    - tests/teardown.sh
